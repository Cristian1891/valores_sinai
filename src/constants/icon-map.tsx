import { InstagramIcon, FacebookIcon, XIcon } from '../components/ui/icons/index';

export const ICON_MAP: Record<string, React.ReactNode> = {
  instagram: <InstagramIcon />,
  facebook:  <FacebookIcon />,
  x:         <XIcon />,
};