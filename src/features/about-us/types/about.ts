export interface InitialsAvatarProps {
  initials: string;
  size?: 'md' | 'lg';
  className?: string;
}

export interface Valor {
  id: string;
  name: string;
  description: string;
  imageUrl: string;
  imageAlt: string;
}